import { _decorator, Component, director, Node, find } from "cc";
const { ccclass, property } = _decorator;

@ccclass("retryBtn")
export class retryBtn extends Component {
  start() {}

  update(deltaTime: number) {}
}
